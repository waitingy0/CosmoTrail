//行星运动算法
export default class MoveAlgorithm {
	
	moveBodies() {
		console.log('Move function undefined');// eslint-disable-line
	}

	//precompute all variations of deltaT, only if it changed since last pass
	// 预先计算deltaT的所有变量，仅当它自上次传递以来发生变化时
	
}
